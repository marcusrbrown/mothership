# mothership

## Unreleased

Changelog entries are generated by [Changesets](https://github.com/changesets/changesets)
when a version bump lands (`bunx changeset version`). See `.changeset/README.md` for how
version PRs are created and how the version stays synchronized across `package.json`,
`src-tauri/tauri.conf.json`, and `src-tauri/Cargo.toml`.
